const input = documents.getElementById('input');
const form = documents.getElementById('form');

form.addEventListener('submit ', (e) => {
    e.preventDefault();

    const todo = input.value;

    if (todo) {

    }
});